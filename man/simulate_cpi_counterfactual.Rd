% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cpi_counterfactual.R
\name{simulate_cpi_counterfactual}
\alias{simulate_cpi_counterfactual}
\title{Simulate CPI counterfactuals from simple policy parameters}
\usage{
simulate_cpi_counterfactual(
  cpi = NULL,
  coicop,
  type = c("vat", "ratio", "unit_subsidy", "index"),
  start,
  end,
  old_rate = NULL,
  new_rate = NULL,
  ratio = NULL,
  elasticity = 1,
  subsidy = NULL,
  unit_price = NULL,
  counterfactual_index = NULL,
  recalculate_price_basket = FALSE,
  recode_ecoicop2_to_ecoicop1 = FALSE
)
}
\arguments{
\item{cpi}{Optional \code{"cpi"} object. If omitted, the function looks for an
object named \code{cpi} or \code{cpi_obj} in the calling environment.}

\item{coicop}{COICOP code to simulate.}

\item{type}{Counterfactual type. One of \code{"vat"}, \code{"ratio"},
\code{"unit_subsidy"}, or \code{"index"}.}

\item{start, end}{Start and end month of the counterfactual window. Values can
be \code{"YYYY-MM"} strings or \code{Date} objects.}

\item{old_rate, new_rate}{VAT rates for \code{type = "vat"}, expressed as
decimals. For example, use \code{0.21} and \code{0.06}, not \code{21} and \code{6}.}

\item{ratio}{Counterfactual-to-observed price ratio for \code{type = "ratio"}.}

\item{elasticity}{Elasticity applied to \code{ratio} for tariff pass-through
cases. Defaults to \code{1}.}

\item{subsidy}{Unit subsidy to remove from the observed price for
\code{type = "unit_subsidy"}.}

\item{unit_price}{Observed unit price for \code{type = "unit_subsidy"}. It can be
a scalar or a data frame/data table with columns \code{date} and \code{unit_price}.}

\item{counterfactual_index}{Counterfactual index for \code{type = "index"}. It can
be a scalar or a data frame/data table with columns \code{date} and
\code{counterfactual_index}.}

\item{recalculate_price_basket}{Whether to recalculate the aggregate price
basket after changing item indices.}

\item{recode_ecoicop2_to_ecoicop1}{Whether to map ECOICOP v2 HICP item codes
back to ECOICOP v1-style codes before applying the counterfactual.}
}
\value{
A \code{"cpi"} object with counterfactual CPI item indices.
}
\description{
\code{simulate_cpi_counterfactual()} applies manually specified counterfactual
policy parameters to one CPI item index. It is designed for cases where the
policy effect can be represented as a monthly price ratio, such as VAT cuts,
unit subsidies, tariff ratios, or an externally supplied counterfactual
index.
}
\examples{
dt <- data.table::data.table(
  series_name = "CPI",
  coicop = "0451",
  value = 100,
  year = rep(2022, 12),
  month = 1:12
)
dt_basket <- data.table::data.table(
  series_name = "CPI",
  value = 100,
  year = rep(2022, 12),
  month = 1:12
)
cpi <- cpi(dt, dt_basket, "FR", 3)
simulate_cpi_counterfactual(
  cpi,
  coicop = "0451",
  type = "vat",
  start = "2022-03",
  end = "2022-12",
  old_rate = 0.21,
  new_rate = 0.06
)

}
