% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_inflation_burden.R
\name{calculate_inflation_burden}
\alias{calculate_inflation_burden}
\title{Calculate inflation burden by household group}
\usage{
calculate_inflation_burden(
  inflation,
  consumption_to_income = NULL,
  expenditure = NULL,
  include_expenditure = TRUE,
  expenditure_unit = "PPS_HH"
)
}
\arguments{
\item{inflation}{An object of class \code{"inflation"} returned by
\code{\link[=calculate_inflation]{calculate_inflation()}}.}

\item{consumption_to_income}{Optional \code{data.table} with columns \code{category},
\code{year}, and \code{consumption_to_income}, where \code{consumption_to_income} is a
percentage of disposable income. If \code{NULL} and \code{inflation$category} is
\code{"income"}, Eurostat \code{icw_sr_10} is loaded.}

\item{expenditure}{Optional \code{data.table} with columns \code{category}, \code{year}, and
\code{expenditure}. If \code{NULL} and \code{include_expenditure = TRUE},
Eurostat \code{hbs_exp_t133} is loaded.}

\item{include_expenditure}{Whether to include \code{expenditure} and
\code{inflation_cost} in the result.}

\item{expenditure_unit}{Eurostat unit used when loading expenditure.}
}
\value{
An object of class \code{"inflation_burden"} containing a \code{data.table}
with monthly inflation, consumption-to-income ratios, inflation burden,
and optionally expenditure and inflation cost.
}
\description{
\code{calculate_inflation_burden()} combines group-specific inflation rates with
either consumption-to-income ratios, mean consumption expenditure, or both.
The default Eurostat path is available for income quintiles.
}
\details{
If \code{consumption_to_income} is available, \code{inflation_burden} is expressed as a
percentage of disposable income:

\code{inflation_burden = inflation * consumption_to_income / 100}

If \code{expenditure} is available, \code{inflation_cost} is expressed in the same unit
as expenditure:

\code{inflation_cost = expenditure * inflation / 100}

Annual Eurostat values are matched to monthly inflation with the latest
available year not later than the inflation year.
}
\examples{
\dontrun{
inflation <- calculate_inflation("FR", "income", level = 2, start_year = 2019)
burden <- calculate_inflation_burden(inflation)
plot_inflation_burden(burden)
}

}
