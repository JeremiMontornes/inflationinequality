% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index_weights.R
\name{index_weights}
\alias{index_weights}
\title{Constructor for a CPI weight data object}
\usage{
index_weights(
  dt = data.table::data.table(),
  country = character(),
  level = numeric(),
  base_total = numeric()
)
}
\arguments{
\item{dt}{a \code{data.table} object (see details).}

\item{country}{2-digit country code (see ISO 3166-1 alpha-2), only one
country at a time is accepted.}

\item{level}{COICOP level. Possible values are 1-3. For example, "01" is
level 1 (Division), "012" is level 2 (Group), and "0111" is level 3
(Class).}

\item{base_total}{Nominal sum of index weights per year. For example, if the
weights are in percentages points, then \code{base_total} is 100. If they are in
per mille, then \code{base_total} is 1000.}
}
\value{
An object of class \code{"index_weights"} is a list containing the
following components:
\itemize{
\item \code{dt}: a \code{data.table} object (see details), each row in \code{dt} represents the
weighting for a specific item category in the CPI at a particular point in
time (see details).
observation for a specific item category
\item \code{country}: 2-digit country code (see ISO 3166-1 alpha-2).
\item \code{level}: COICOP level.
\item \code{start_year}: first year of data.
\item \code{end_year}: last year of data.
}
}
\description{
\code{index_weights()} constructs an object of class
\code{"index_weights"} which contains annual CPI (Consumer Price Index) weight
data of products categorized by their COICOP code from a single country.
}
\details{
The component \code{dt} has the following columns:
\itemize{
\item \code{coicop}: COICOP code.
\item \code{weight}: weight of item category at specified time.
\item \code{year}: year of data series.
}

The weights of all item categories in a given year must be normalized to
100.
}
