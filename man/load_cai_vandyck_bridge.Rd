% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shock_bridge.R
\name{load_cai_vandyck_bridge}
\alias{load_cai_vandyck_bridge}
\title{Load Cai-Vandyck COICOP-to-CPA bridge matrices}
\usage{
load_cai_vandyck_bridge(
  path = NULL,
  url = "https://ars.els-cdn.com/content/image/1-s2.0-S2352340920302894-mmc1.xlsx",
  cache_dir = tools::R_user_dir("inflationinequality", "cache"),
  countries = NULL,
  figaro_64 = TRUE,
  keep_value = FALSE
)
}
\arguments{
\item{path}{Local path to the Cai-Vandyck workbook. If \code{NULL}, \code{url} is
downloaded to \code{cache_dir}.}

\item{url}{URL of the workbook to download when \code{path} is \code{NULL}.}

\item{cache_dir}{directory used to cache the downloaded workbook.}

\item{countries}{optional character vector of country sheets to keep.}

\item{figaro_64}{if \code{TRUE}, recode the few Cai-Vandyck CPA labels that differ
from the FIGARO 64-sector labels used elsewhere in the package
(\code{D -> D35}, \code{L68 -> L}, \code{O -> O84}, \code{P -> P85}).}

\item{keep_value}{if \code{TRUE}, keep the original bridge value before
normalisation.}
}
\value{
A data.table with columns \code{country}, \code{coicop}, \code{sector}, and
\code{share}; \code{sector} is the CPA code without the \code{CPA_} prefix.
}
\description{
Loads the country-specific bridge matrices from Cai and Vandyck (2020),
"Bridging between economy-wide activity and household-level consumption
data: Matrices for European countries". The workbook stores CPA products in
rows and 35 COICOP consumption categories in columns. This function converts
each country sheet to long form and normalises CPA values within each
country-COICOP column.

The returned table can be passed directly to \code{\link[=simulate_shock]{simulate_shock()}} as \code{bridge}.
}
\examples{
\dontrun{
bridge <- load_cai_vandyck_bridge(countries = c("FR", "DE"))
}

}
\references{
Cai, M. and Vandyck, T. (2020). Bridging between economy-wide activity and
household-level consumption data: Matrices for European countries. Data in
Brief, 30, 105395. doi:10.1016/j.dib.2020.105395.
}
