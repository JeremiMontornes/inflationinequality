% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_to_official_hicp.R
\name{compare_to_official_hicp}
\alias{compare_to_official_hicp}
\title{Compare calculated average HICP with published HICP}
\usage{
compare_to_official_hicp(
  inflation,
  country = NULL,
  custom_cpi = NULL,
  measure = NULL
)
}
\arguments{
\item{inflation}{An object of class \code{"inflation"} or \code{"price_indices"}.}

\item{country}{Optional 2-digit country code. If \code{NULL}, the country stored
in \code{inflation} is used.}

\item{custom_cpi}{Optional object of class \code{"cpi"} containing the official
all-items basket in \code{dt_basket}. If \code{NULL}, the function downloads HICP
data with \code{\link[=load_cpi]{load_cpi()}}.}

\item{measure}{validation measure. If \code{NULL}, \code{"rate"} is used for
\code{"inflation"} objects and \code{"level"} for \code{"price_indices"} objects.}
}
\value{
An object of class \code{"hicp_comparison"} containing:
\itemize{
\item \code{dt}: date-by-date comparison between calculated and published HICP.
\item \code{summary}: validation statistics.
\item \code{plot}: a ggplot object with grey bars for the date-by-date difference.
\item \code{country}: country code.
\item \code{measure}: comparison measure (\code{"rate"} or \code{"level"}).
}
}
\description{
\code{compare_to_official_hicp()} validates the average HICP calculated from
household-category results against the published all-items HICP.
}
\details{
With an \code{"inflation"} object, the calculated average is obtained with
\code{\link[=calculate_total_inflation]{calculate_total_inflation()}}, ie the mean across household categories, and
compared with the year-on-year rate of the published all-items HICP.

With a \code{"price_indices"} object, the calculated average is the mean of
household-category price indices. A \code{"Total"} category, if present, is
excluded from the average. The published all-items HICP is rebased to the
same base year before comparison.
}
\examples{
\dontrun{
inflation <- calculate_inflation("FR", "income", start_year = 2019)
comparison <- compare_to_official_hicp(inflation)
comparison$plot
comparison$summary

indices <- calculate_price_indices("FR", "income", start_year = 2019, base_year = 2010)
level_comparison <- compare_to_official_hicp(indices)
level_comparison$plot
}

}
