% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cpi.R
\name{simulate_cpi}
\alias{simulate_cpi}
\title{Simulate CPI data}
\usage{
simulate_cpi(
  cpi_obj,
  simulations,
  recalculate_price_basket = FALSE,
  recode_ecoicop2_to_ecoicop1 = FALSE
)
}
\arguments{
\item{cpi_obj}{a "cpi" object.}

\item{simulations}{a data.frame or data.table}

\item{recalculate_price_basket}{flag to recalculate the price index of the product basket}

\item{recode_ecoicop2_to_ecoicop1}{whether to map ECOICOP v2 HICP item
codes back to ECOICOP v1-style codes before applying simulations and
matching index weights.}
}
\value{
a "cpi" object with the simulated CPI data.
}
\description{
\code{simulate_cpi} modifies the price index of specified product categories under specified conditions. For example, you may want to simulate the price index of petrol to be increased by 20\% from April 2022 to November 2022.

The simulation is computed on unchained item indices, following the same
chain-linking convention as \code{\link[=calculate_price_indices]{calculate_price_indices()}}. Item indices are
first converted to unchained annual price relatives with \code{\link[hicp:chaining]{hicp::unchain()}}.
The requested counterfactual level is then translated back into unchained
movements, the product basket is optionally re-aggregated with annual index
weights, and simulated levels are rebuilt with \code{\link[hicp:chaining]{hicp::chain()}}.

The formula to calculate the simulated level during the simulated period is:
sim_index_t = index_starttime * (1 + shock)

The product basket is recalculated as a weighted Laspeyres aggregate of
unchained item movements before chain-linking.
}
\details{
\code{simulations} will have the following component:
\itemize{
\item \code{coicop}: the COICOP code
\item \code{shock}: the change in the COICOP code's CPI by percentage, must be at least -100.
\item \code{start_year}: start year
\item \code{start_month}: start month
\item \code{end_year}: end year
\item \code{end_month}: end month
}
}
