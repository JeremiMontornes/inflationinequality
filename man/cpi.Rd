% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpi.R
\name{cpi}
\alias{cpi}
\title{Constructor for a CPI data object}
\usage{
cpi(
  dt = data.table::data.table(),
  dt_basket = data.table::data.table(),
  country = character(),
  level = numeric()
)
}
\arguments{
\item{dt}{a \code{data.table} object (see details).}

\item{dt_basket}{a \code{data.table} object (see details).}

\item{country}{2-digit country code (see ISO 3166-1 alpha-2), only one
country at a time is accepted.}

\item{level}{COICOP level. Possible values are 1-3. For example, "01" is
level 1 (Division), "012" is level 2 (Group), and "0111" is level 3
(Class).}
}
\value{
An object of class \code{"cpi"} is a list containing the following components:
\itemize{
\item \code{dt}: a \code{data.table} object, each row in \code{dt} represents a single price
observation for a specific item category at a particular point in time (see
details).
\item \code{dt_basket}: a \code{data.table} object, each row in \code{dt} represents a single
price observation for the entire price basket at a particular point in time
(see details).
\item \code{country}: 2-digit country code (see ISO 3166-1 alpha-2).
\item \code{level}: COICOP level.
\item \code{start_year}: first year of data.
\item \code{start_month}: first month of data.
\item \code{end_year}: last year of data.
\item \code{end_month}: last month of data.
}
}
\description{
\code{cpi()} constructs an object of class \code{"cpi"} which contains monthly CPI
(Consumer Price Index) data of products categorized by their COICOP code from
a single country.
}
\details{
The component \code{dt} contains the following columns:
\itemize{
\item \code{series_name}: identifier for the data series.
\item \code{coicop}: COICOP (Classification of Individual Consumption by Purpose)
code.
\item \code{value}: price of item category at the specified time.
\item \code{year}: year of the data series.
\item \code{month}: month of the data series.
}

The component \code{dt_basket} has the following columns:
\itemize{
\item \code{series_name}: identifier for the data series.
\item \code{value}: price of item category at the specified time.
\item \code{year}: year of the data series.
\item \code{month}: month of the data series.
}

For each (year, month) pair in \code{dt}, there should be a corresponding
(year, month) pair in \code{dt_basket}.
}
