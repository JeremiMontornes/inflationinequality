% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hbs.R
\name{hbs}
\alias{hbs}
\title{Constructor for HBS data object}
\usage{
hbs(
  dt = data.table::data.table(),
  dt_total = data.table::data.table(),
  country = character(),
  category = character(),
  categories = vector(),
  level = numeric()
)
}
\arguments{
\item{dt}{a \code{data.table} object (see details).}

\item{dt_total}{a \code{data.table} object (see details).}

\item{country}{2-digit country code (see ISO 3166-1 alpha-2), only one
country at a time is accepted.}

\item{category}{HBS category: \code{"income"}, \code{"age"}, \code{"urban"}.}

\item{categories}{(Ordered) vector of category types, from lowest to highest.}

\item{level}{COICOP level. Possible values are 1-3. For example, "01" is
level 1 (Division), "012" is level 2 (Group), and "0111" is level 3
(Class).}
}
\value{
An object of class \code{"hbs"} is a list containing the following
components:
\itemize{
\item \code{dt}: a \code{data.table} object, each row in \code{dt} represents the weighting
of a specific item category in a demographic category in a particular year
(see details).
\item \code{dt_total}: a \code{data.table} object, each row in \code{dt} represents the
weighting of a specific item category for the entire population in a
particular year (see details).
\item \code{country}: 2-digit country code (see ISO 3166-1 alpha-2).
\item \code{category}: HBS category: \code{"income"}, \code{"age"}, \code{"urban"}.
\item \code{categories}: (Ordered) vector of category types, from lowest to highest.
\item \code{level}: COICOP level.
\item \code{start_year}: first year of data.
\item \code{end_year}: last year of data.
}
}
\description{
\code{hbs()} constructs an object of class \code{"hbs"} which contains HBS (Household
Budget Survey) data.
}
\details{
The component \code{dt} has the following columns:
\itemize{
\item \code{series_name}: identifier for the data series.
\item \code{coicop}: COICOP code.
\item \code{year}: year of data series.
\item \code{category}: category type within the specified HBS category.
\item \code{consumption}: consumption value for the specified category and COICOP
code.
}

For every (COICOP, year) pair, there should be 1 row for each available
category.

The component \code{dt_total} has the following columns:
\itemize{
\item \code{series_name}: identifier for the data series.
\item \code{coicop}: COICOP code.
\item \code{year}: year of data series.
\item \code{total_consumption}: consumption value for the specified category and
COICOP code.
}

For each (COICOP, year) pair in \code{dt}, there should be a corresponding
(COICOP, year) pair in \code{dt_total}.
}
\examples{
# Create a sample HBS dataset
dt <- data.table(
  series_name = c("HBS", "HBS", "HBS", "HBS"),
  coicop = c("01", "02", "01", "02"),
  year = c(2022, 2022, 2023, 2023),
  category = c("A", "A", "B", "B"),
  consumption = c(100, 200, 150, 250)
)

dt_total <- data.table(
  coicop = c("01", "02", "01", "02"),
  year = c(2022, 2022, 2023, 2023),
  total_consumption = c(300, 400, 350, 450)
)

# Create an HBS object
my_hbs <- hbs(dt = dt,
              dt_total = dt_total,
              country = "FR",
              category = "income",
              categories = c("A", "B"),
              level = 1)

}
