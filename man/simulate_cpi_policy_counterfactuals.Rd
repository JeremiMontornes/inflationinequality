% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cpi_counterfactual.R
\name{simulate_cpi_policy_counterfactuals}
\alias{simulate_cpi_policy_counterfactuals}
\title{Simulate several CPI policy counterfactuals}
\usage{
simulate_cpi_policy_counterfactuals(
  cpi,
  policies,
  recalculate_price_basket = TRUE,
  recode_ecoicop2_to_ecoicop1 = FALSE
)
}
\arguments{
\item{cpi}{A \code{"cpi"} object.}

\item{policies}{A data.frame or data.table of monthly counterfactual policy
rows, usually created with \code{\link[=cpi_counterfactual_policy]{cpi_counterfactual_policy()}}. Required columns
are \code{coicop}, \code{date}, \code{policy_ratio}, and \code{counterfactual_index}.}

\item{recalculate_price_basket}{Whether to recalculate the aggregate price
basket after changing item indices.}

\item{recode_ecoicop2_to_ecoicop1}{Whether to map ECOICOP v2 HICP item codes
back to ECOICOP v1-style codes before applying the counterfactual.}
}
\value{
A \code{"cpi"} object with counterfactual CPI item indices and, when
\code{recalculate_price_basket = TRUE}, a recalculated aggregate CPI basket.
}
\description{
\code{simulate_cpi_policy_counterfactuals()} applies several monthly policy
counterfactuals to CPI item indices, then optionally recalculates the
aggregate CPI basket once. This is useful for policy scenarios combining
several VAT cuts, unit subsidies, tariff ratios, or externally supplied
counterfactual indices.
}
\examples{
dt <- data.table::data.table(
  series_name = rep("CPI", 24),
  coicop = rep(c("0451", "0452"), each = 12),
  value = 100,
  year = rep(2022, 24),
  month = rep(1:12, 2)
)
dt_basket <- data.table::data.table(
  series_name = "CPI",
  value = 100,
  year = rep(2022, 12),
  month = 1:12
)
cpi <- cpi(dt, dt_basket, "FR", 3)
policies <- data.table::rbindlist(list(
  cpi_counterfactual_policy("vat_electricity", "0451", "vat", "2022-03", "2022-12", old_rate = 0.21, new_rate = 0.06),
  cpi_counterfactual_policy("vat_gas", "0452", "vat", "2022-03", "2022-12", old_rate = 0.21, new_rate = 0.06)
))
simulate_cpi_policy_counterfactuals(cpi, policies, recalculate_price_basket = FALSE)

}
