% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_contributions.R
\name{calculate_contributions}
\alias{calculate_contributions}
\title{Calculate contributions to annual inflation}
\usage{
calculate_contributions(
  country = NULL,
  category = NULL,
  level = 2,
  start_year = NULL,
  start_month = NULL,
  end_year = NULL,
  end_month = NULL,
  ensure_complete_cpi = FALSE,
  custom_cpi = NULL,
  custom_index_weights = NULL,
  custom_hbs = NULL,
  interpolated_hbs = FALSE,
  specific_hbs_year = NULL,
  france_insee_income_groups = c("decile", "quintile"),
  weighting_method = c("relative_expenditure", "ras", "additive_qp"),
  recode_ecoicop2_to_ecoicop1 = TRUE
)
}
\arguments{
\item{country}{2-digit country code (see ISO 3166-1 alpha-2), only one
country at a time is accepted. The parameter is optional since we can load
\code{custom_index_weights} and \code{custom_hbs}, but if both of those arguments are
missing, then the \code{country} parameter is mandatory.}

\item{category}{HBS data by category: \code{"income"}, \code{"age"}, \code{"urban"}. The
parameter is optional since one can load \code{custom_hbs}, but it is mandatory
if that argument is missing.}

\item{level}{COICOP level. Possible values are 1-3. For example, "01" is
level 1 and "012" is level 2. Default value is 2.}

\item{start_year}{year of start date.}

\item{start_month}{month of start date.}

\item{end_year}{year of end date.}

\item{end_month}{month of end date.}

\item{ensure_complete_cpi}{flag that when set to \code{TRUE}, synthesizes missing
CPI data when the CPI dataset is incomplete.}

\item{custom_cpi}{an object of class \code{"cpi"}}

\item{custom_index_weights}{an object of class \code{"index_weights"}.}

\item{custom_hbs}{an object of class \code{"hbs"}.}

\item{interpolated_hbs}{flag if you want to interpolate HBS weights}

\item{specific_hbs_year}{year of selected HBS wave. It's recommended to
download HBS data first to see what HBS years are available.}

\item{france_insee_income_groups}{grouping used for the bundled France
INSEE level-3 income HBS data. \code{"decile"} keeps the original INSEE decile
means. \code{"quintile"} averages adjacent decile means (D1-D2, D3-D4, ...)
because the source values are mean expenditures for equal-population
decile groups, not income-threshold bounds. This option only affects
\code{category = "income"}; France \code{age} and \code{urban} level-3 HBS data keep their
original national groups.}

\item{weighting_method}{weighting formula. \code{"relative_expenditure"} keeps the
historical package formula. \code{"ras"} applies iterative proportional fitting.
\code{"additive_qp"} preserves absolute HBS differences between groups and, when
necessary, projects the resulting weights onto the non-negative weights
satisfying the group-basket and HICP-item margins. At COICOP level 2, all
weighting methods combine HBS actual rents (\code{04.1}) and imputed rents of
owner-occupiers (\code{04.2}) to distribute the HICP actual-rents weight
(\code{04.1}). This housing bridge is applied for every country when both HBS
components are available; HICP weights remain unchanged.}

\item{recode_ecoicop2_to_ecoicop1}{whether to map ECOICOP v2 HICP item
codes back to ECOICOP v1-style codes before matching them to HBS weights.
If \code{NULL}, the default \code{TRUE} is used.}
}
\value{
An object of class \code{"contributions"} is a list containing the
following components:
\itemize{
\item \code{dt}: a \code{data.table} object (see below).
\item \code{dt_missing_weights}: a \code{data.table} object (see below).
\item \code{dt_coverage}: a \code{data.table} object (see below).
\item \code{dt_effective_weights}: retained weights after renormalization.
\item \code{dt_price_indices}: the exact price-index observations being decomposed.
\item \code{country}: 2-digit country code (see ISO 3166-1 alpha-2).
\item \code{category}: Category for which contributions were calculated: "income",
"age", or "urban".
\item \code{categories}: (Ordered) vector of category types, from lowest to highest.
\item \code{level}: COICOP level.
\item \code{start_year}: first year of data.
\item \code{start_month}: first month of data.
\item \code{end_year}: last year of data.
\item \code{end_month}: last month of data.
}

The component \code{dt} has the following columns:
\itemize{
\item \code{year}: year of the contribution.
\item \code{coicop}: COICOP code.
\item \code{month}: month of the contribution.
\item \code{category}: demographic category (e.g., income group, age group).
\item \code{contribution}: calculated contribution to inflation.
}

The component \code{dt_missing_weights} has the following columns:
\describe{
\item{coicop}{COICOP code}
\item{year}{year}
\item{category}{demographic category}
\item{missing_weight}{weight of product category}
}

The component \code{dt_coverage} has the following columns:
\describe{
\item{year}{weight year}
\item{original_weight}{weight mass before filtering}
\item{included_weight}{weight mass with a valid price path}
\item{excluded_weight}{weight mass excluded from the common universe}
\item{coverage_rate}{included weight divided by original weight}
}
}
\description{
\code{calculate_contributions()} decomposes the exact annual inflation rates
returned by \code{\link[=calculate_price_indices]{calculate_price_indices()}} into elementary COICOP contributions.
}
\details{
The annual rate crosses two chain links: the movement from month \code{m} of
\code{y - 1} to December of \code{y - 1}, and the movement from that December to month
\code{m} of \code{y}. The interaction between the two links is allocated symmetrically.
Products must have both prices and weights throughout a calendar year;
retained weights are renormalized to 100 and excluded mass is reported as a
coverage diagnostic. Missing prices or weights are never replaced by a
positive artificial weight.
}
\examples{
\dontrun{
# Calculate inflation contributions for France, income category, COICOP
# level 2, from 2010 to 2020
france_contributions <- calculate_contributions(
  "FR", "income", level = 2, start_year = 2010, end_year = 2020
)

# Access the data.table component
dt_contributions <- france_contributions$dt

# Sum contributions for a specific year, month, and category
total_contribution <- dt_contributions[
  year == 2015 & month == 6 & category == "First quintile",
  sum(contribution)
]
}

}
\seealso{
\code{\link[=calculate_price_indices]{calculate_price_indices()}}, \code{\link[=calculate_weights]{calculate_weights()}},
\code{\link[=load_cpi]{load_cpi()}}
}
