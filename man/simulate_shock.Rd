% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_shock}
\alias{simulate_shock}
\title{Simulate an input-output supply shock and inflation inequality effects}
\usage{
simulate_shock(
  A,
  nodes,
  shock = 0.1,
  shock_sectors = c("B", "C19", "D35"),
  shock_scope = c("all", "domestic", "imported"),
  bridge,
  countries = NULL,
  aggregate_geo = "EA20",
  country_weights = NULL,
  total_weights = NULL,
  group_weights = list(),
  gap_categories = list(),
  output_scale = 100
)
}
\arguments{
\item{A}{square input-output technical-coefficient matrix. Rows are input
sectors and columns are producing sectors. Row and column names must match.}

\item{nodes}{data.frame with one row per row/column of \code{A}. Required columns
are \code{node}, \code{country}, and \code{sector}.}

\item{shock}{numeric scalar or named vector. If scalar, it is applied to all
rows selected by \code{shock_sectors}. If named, names must match node names.}

\item{shock_sectors}{sectors receiving the direct shock. Defaults to common
FIGARO energy sectors: mining and quarrying, coke/refined petroleum, and
electricity/gas.}

\item{shock_scope}{scope used when \code{shock} is a scalar. \code{"all"} shocks all
selected sectors in the matrix, \code{"domestic"} shocks selected sectors in the
reporting country only, and \code{"imported"} shocks selected sectors outside
the reporting country. \code{"imported"} is useful for imported energy input
price shocks.}

\item{bridge}{data.frame mapping consumption COICOP codes to NACE/FIGARO
sectors. Required columns are \code{coicop}, \code{sector}, and optionally \code{share}.
If \code{share} is missing, equal shares are used within each COICOP.}

\item{countries}{country codes to report. Defaults to all countries in
\code{nodes}.}

\item{aggregate_geo}{name of the aggregate row. Defaults to \code{"EA20"}.}

\item{country_weights}{optional data.frame with columns \code{country} and
\code{country_weight}, used to aggregate countries into \code{aggregate_geo}.}

\item{total_weights}{optional data.frame of average consumption weights with
columns \code{country}, \code{coicop}, and \code{weight}.}

\item{group_weights}{named list of data.frames for inequality groups. Each
element name is an outcome name, for example
\code{inflation_gap_revenu}, \code{inflation_gap_age}, or \code{inflation_gap_area}.
Each data.frame must contain \code{country}, \code{coicop}, \code{category}, and \code{weight}.}

\item{gap_categories}{optional named list. Each element is a length-two
character vector defining the low/reference group and high/comparison
group for the matching \code{group_weights} element.}

\item{output_scale}{multiplier applied to inflation effects. Use \code{100} to
express a 0.01 price change as 1 percentage point. Defaults to \code{100}.}
}
\value{
A data.table with one row per country plus the aggregate row.
}
\description{
\code{simulate_shock()} propagates a sectoral unit-cost shock through an
input-output price model,
\code{delta_p = solve(I - t(A), s)}, then aggregates the resulting sectoral price
changes with household consumption weights. It reports the average inflation
effect and inflation gaps for income, age, and area groups, by country and for
an aggregate such as the euro area.
}
