% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpi.R
\name{get_missing_cpi_tuples}
\alias{get_missing_cpi_tuples}
\title{Search for missing CPI data}
\usage{
get_missing_cpi_tuples(cpi)
}
\arguments{
\item{cpi}{an object of class \code{"cpi"}.}
}
\value{
a \code{data.table} object with the following columns:
\itemize{
\item \code{coicop}: COICOP code.
\item \code{year}: year.
\item \code{month}: month.
}
}
\description{
\code{get_missing_cpi_tuples()} searches for the years and months where a product
does not have CPI data.
}
\examples{
# Download CPI data from Eurostat
cpi <- load_cpi("FR")

# Find missing data
missing_data <- get_missing_cpi_tuples(cpi)

if (length(missing_data) > 0) {
  message("There is data missing in French CPI data!")
} else {
  message("There is no data missing in French CPI data.")
}

}
