% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_household_inflation.R
\name{calculate_household_inflation}
\alias{calculate_household_inflation}
\title{Calculate household-level inflation from Spanish EPF microdata}
\usage{
calculate_household_inflation(
  country = "ES",
  years = 2020:2024,
  level = 3,
  start_year = NULL,
  start_month = NULL,
  end_year = NULL,
  end_month = NULL,
  zip_dir = spain_epf_zip_dir(),
  zip_paths = NULL,
  download = FALSE,
  overwrite = FALSE,
  base_year = NULL,
  include_price_indices = FALSE,
  recode_ecoicop2_to_ecoicop1 = TRUE,
  ensure_complete_cpi = FALSE
)
}
\arguments{
\item{country}{Country code. Only \code{"ES"} is currently implemented.}

\item{years}{EPF microdata years to use. The default is \code{2020:2024}.}

\item{level}{COICOP level. Defaults to \code{3}, i.e. 4-digit class level.}

\item{start_year, start_month, end_year, end_month}{date range for inflation.}

\item{zip_dir}{directory where INE EPF \code{datos_YYYY.zip} files are stored.}

\item{zip_paths}{optional named or unnamed vector of explicit EPF ZIP paths.
When supplied, it is used instead of \code{zip_dir}/\code{download}. File names can
follow either INE's \code{datos_YYYY.zip} convention or a local convention such
as \code{ine_epf_YYYY.zip}.}

\item{download}{if \code{TRUE}, download missing ZIP files from INE.}

\item{overwrite}{if \code{TRUE}, re-download existing ZIP files.}

\item{base_year}{year used to rebase chained price indices.}

\item{include_price_indices}{if \code{TRUE}, include the full price-index object
in the returned list.}

\item{recode_ecoicop2_to_ecoicop1}{passed to \code{\link[=calculate_price_indices]{calculate_price_indices()}}.}

\item{ensure_complete_cpi}{passed to \code{\link[=calculate_price_indices]{calculate_price_indices()}}.}
}
\value{
An object of class \code{"household_inflation"} containing:
\itemize{
\item \code{dt}: household-month inflation rates with columns \code{household_id},
\code{year}, \code{month}, \code{date}, \code{inflation}, \code{price_index}, and HBS metadata.
\item \code{households}: EPF household metadata.
\item \code{hbs}: household-level HBS object used in the calculation.
\item \code{price_indices}: included only when \code{include_price_indices = TRUE}.
}
}
\description{
\code{calculate_household_inflation()} builds household-specific HICP Laspeyres
indices by combining INE EPF household expenditure microdata with Eurostat
HICP item indices and official HICP item weights.
}
\details{
The function currently supports Spain (\code{country = "ES"}). It expects the INE
EPF ZIP files \code{datos_YYYY.zip} in \code{zip_dir}. If \code{download = TRUE}, missing
ZIPs are downloaded from the official INE microdata endpoint.

For each household and EPF wave, the function reads \code{EPFhogar_YYYY.csv} and
\code{EPFgastos_YYYY.csv}, keeps household weights and expenditure lines, builds
a household-level HBS object, and passes it to \code{\link[=calculate_price_indices]{calculate_price_indices()}}.
Household baskets are matched to the official HICP annual item weights using
the same machinery as \code{\link[=calculate_weights]{calculate_weights()}}. The output therefore follows the
package convention: EPF waves are used as HBS waves and matched to HICP
weight years by latest available prior wave.
}
\examples{
\dontrun{
es_hh <- calculate_household_inflation(
  years = 2020:2024,
  start_year = 2021,
  end_year = 2024,
  download = TRUE
)
es_hh$dt
}

}
\seealso{
\code{\link[=calculate_price_indices]{calculate_price_indices()}}, \code{\link[=calculate_weights]{calculate_weights()}}
}
